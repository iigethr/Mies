/* Danger zone */

.danger_zone
  .danger_zone-box
    .danger_zone-item
      %h3.text-center Danger Zone
      %p.text-center Once deleted, it will be gone forever. Please be certain.
      = link_to "#", method: :delete, data: { confirm: "Are you sure?" }, class: "button large red altai-center" do
        Destroy this "item"
